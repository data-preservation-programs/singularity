# Put.io

{% code fullWidth="true" %}
```
NAME:
   singularity storage create putio - Put.io

USAGE:
   singularity storage create putio [command options] [arguments...]

DESCRIPTION:
   --encoding
      后端的编码方式。
      
      更多信息请参阅[概述](/overview/#encoding)中的编码部分。


OPTIONS:
   --help, -h  显示帮助信息

   高级选项

   --encoding value  后端的编码方式。（默认值：Slash,BackSlash,Del,Ctl,InvalidUtf8,Dot）[$ENCODING]

   通用选项

   --name value  存储的名称（默认值：自动生成）
   --path value  存储的路径

```
{% endcode %}
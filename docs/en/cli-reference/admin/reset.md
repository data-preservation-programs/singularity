# Reset the database

{% code fullWidth="true" %}
```
NAME:
   singularity admin reset - Reset the database

USAGE:
   singularity admin reset [command options] [arguments...]

OPTIONS:
   --help, -h  show help
```
{% endcode %}

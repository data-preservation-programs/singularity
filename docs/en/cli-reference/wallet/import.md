# Import a wallet from a private key file into the keystore

{% code fullWidth="true" %}
```
NAME:
   singularity wallet import - Import a wallet from a private key file into the keystore

USAGE:
   singularity wallet import [command options] [path, or stdin if omitted]

OPTIONS:
   --help, -h  show help
```
{% endcode %}
